#include <stdio.h>
#include <string.h>

int n;
char str[10];

int main(){
    freopen("b.in","r",stdin);
    scanf("%d",&n);
    for (int i=1;i<=n;i++){
        scanf("%s",str);
        // printf("%s ",str);

    }

    return 0;
}